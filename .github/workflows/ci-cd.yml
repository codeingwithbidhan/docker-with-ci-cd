name: Docker laravel-react with CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1-6: (Unchanged)
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build & Push Laravel Backend
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          file: ./backend/Dockerfile.production
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/laravel-backend:latest

      - name: Build & Push React Frontend
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile.production
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/react-frontend:latest


      - name: Copy Docker Compose File to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          source: "docker-compose.production.yml" 
          target: "/home/${{ secrets.SERVER_USER }}/docker-with-ci-cd"

      # 💡 Step 7: ফাইল চেক ও ইমেজ পুল করা (SUDO removed)
      - name: Pull Latest Docker Images & Create ENV
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          command_timeout: 20m 

          script: |
            TARGET_DIR=/home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            mkdir -p $TARGET_DIR
            cd $TARGET_DIR
            
            if [ ! -f "docker-compose.production.yml" ]; then
                echo "Error: docker-compose.production.yml file NOT FOUND in $TARGET_DIR";
                ls -la 
                exit 1;
            fi
            
            echo "Creating .env file from secrets..."
            echo "DB_DATABASE=${{ secrets.DB_DATABASE }}" > .env
            echo "DB_USERNAME=${{ secrets.DB_USERNAME }}" >> .env
            echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
            echo "DB_ROOT_PASSWORD=${{ secrets.DB_ROOT_PASSWORD }}" >> .env
            
            docker-compose -f docker-compose.production.yml down || true
            docker image prune -f || true
            
            echo "Starting Image Pull..."
            docker-compose -f docker-compose.production.yml pull
            echo "Image Pull Complete."

      # 🔍 ডায়াগনস্টিক ধাপ: সার্ভারে থাকা ফাইল কনটেন্ট প্রিন্ট করা (Unchanged)
      - name: Verify Docker Compose Content on Server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          
          script: |
            cd /home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            echo "--- VERIFYING CURRENT docker-compose.production.yml CONTENT ---"
            cat docker-compose.production.yml
            echo "---------------------------------------------------------------"

      # 💡 চূড়ান্ত ফিক্স (Step 7.5): কন্টেইনার স্টার্ট এবং মাইগ্রেশন (Single Blocking Step)
      - name: Deploy and Run Migrations (15 Min Timeout)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          # 🚨 চূড়ান্ত টাইমআউট: 15 মিনিট, সম্পূর্ণ স্টার্টআপ এবং মাইগ্রেশনের জন্য।
          command_timeout: 15m 

          script: |
            cd /home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            
            echo "Starting containers (Blocking wait for DB) - up to 15 minutes..."
            # এই কমান্ডটি depends_on: service_started শর্ত পূরণ না হওয়া পর্যন্ত ব্লক থাকবে।
            docker-compose -f docker-compose.production.yml up -d --force-recreate --remove-orphans
            
            echo "--- Container Status Check After Startup ---"
            docker-compose -f docker-compose.production.yml ps
            
            echo "Containers are up. Running Migrations..."
            
            # LARAVEL কন্টেইনারের নাম বের করা
            LARAVEL_CONTAINER_NAME=$(docker-compose -f docker-compose.production.yml ps -q backend) 
            
            if [ -n "$LARAVEL_CONTAINER_NAME" ]; then
              CONTAINER_STATUS=$(docker inspect -f '{{.State.Status}}' $LARAVEL_CONTAINER_NAME)

              if [ "$CONTAINER_STATUS" = "running" ]; then
                  echo "Backend container is RUNNING. Waiting 10 seconds for final DB establishment...";
                  sleep 10
                  
                  # মাইগ্রেশন এবং ক্যাশ ক্লিয়ার
                  docker exec $LARAVEL_CONTAINER_NAME php artisan migrate --force;
                  docker exec $LARAVEL_CONTAINER_NAME php artisan cache:clear;
                  echo "Deployment successful!";
              else
                  echo "Error: Backend container found but status is NOT RUNNING ($CONTAINER_STATUS).";
                  
                  # 🚨 ডিবাগিং: কন্টেইনারটি কেন ব্যর্থ হয়েছে তার লগ প্রিন্ট করা
                  echo "--- Printing Backend Container Logs ---"
                  docker-compose -f docker-compose.production.yml logs backend
                  
                  exit 1;
              fi
            else
              echo "Error: Backend container not found. Containers failed to start after blocking wait.";
              echo "--- Printing All Container Logs ---"
              docker-compose -f docker-compose.production.yml logs 
              exit 1;
            fi
