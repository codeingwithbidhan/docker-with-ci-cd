name: Docker laravel-react with CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1-6: (Unchanged)
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build & Push Laravel Backend
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          file: ./backend/Dockerfile.production
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/laravel-backend:latest

      - name: Build & Push React Frontend
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile.production
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/react-frontend:latest


      - name: Copy Docker Compose File to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          source: "docker-compose.production.yml" 
          target: "/home/${{ secrets.SERVER_USER }}/docker-with-ci-cd"

      # 💡 Step 7: ফাইল চেক ও ইমেজ পুল করা (Unchanged)
      - name: Pull Latest Docker Images & Create ENV
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          command_timeout: 20m 

          script: |
            TARGET_DIR=/home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            mkdir -p $TARGET_DIR
            cd $TARGET_DIR
            
            if [ ! -f "docker-compose.production.yml" ]; then
                echo "Error: docker-compose.production.yml file NOT FOUND in $TARGET_DIR";
                ls -la 
                exit 1;
            fi
            
            echo "Creating .env file from secrets..."
            echo "DB_DATABASE=${{ secrets.DB_DATABASE }}" > .env
            echo "DB_USERNAME=${{ secrets.DB_USERNAME }}" >> .env
            echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
            echo "DB_ROOT_PASSWORD=${{ secrets.DB_ROOT_PASSWORD }}" >> .env
            
            docker-compose -f docker-compose.production.yml down || true
            docker image prune -f || true
            
            echo "Starting Image Pull..."
            docker-compose -f docker-compose.production.yml pull
            echo "Image Pull Complete."

      # 🔍 ডায়াগনস্টিক ধাপ: সার্ভারে থাকা ফাইল কনটেন্ট প্রিন্ট করা (Unchanged)
      - name: Verify Docker Compose Content on Server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          
          script: |
            cd /home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            echo "--- VERIFYING CURRENT docker-compose.production.yml CONTENT ---"
            cat docker-compose.production.yml
            echo "---------------------------------------------------------------"

      # 💡 ডায়াগনস্টিক: সার্ভারের স্বাস্থ্য পরীক্ষা ও পাথ যাচাই (নতুন শক্তিশালী ধাপ)
      - name: Deployment Health Check (Path & Syntax)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}

          script: |
            cd /home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            
            echo "--- Checking Docker Compose Path and Installation ---"
            
            # Check if the explicit path exists
            if [ -f /usr/local/bin/docker-compose ]; then
                echo "SUCCESS: Explicit path /usr/local/bin/docker-compose exists."
            else
                echo "FATAL ERROR: /usr/local/bin/docker-compose not found. Docker Compose might be missing or installed elsewhere."
                exit 1
            fi
            
            echo "--- Validating Docker Compose Syntax (using explicit path) ---"
            # Validate YAML syntax and configuration, force exit on failure
            /usr/local/bin/docker-compose -f docker-compose.production.yml config || {
                echo "FATAL ERROR: docker-compose config failed. Check YAML output for errors."
                exit 1
            }
            echo "Syntax check passed successfully. Ready to launch containers."


      # 💡 সংশোধিত Step 7.5: কন্টেইনার স্টার্ট (Non-Blocking) - SSH হ্যাং এড়াতে nohup-এ প্রত্যাবর্তন
      - name: Start Docker Containers (Non-Blocking)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          # 🚨 টাইমআউট 1 মিনিট, কারণ কমান্ডটি অবিলম্বে ব্যাকগ্রাউন্ডে চলে যাবে
          command_timeout: 1m 

          script: |
            cd /home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            
            echo "Starting containers using nohup for non-blocking execution with explicit path..."
            # 🚨 FIX: docker-compose এর পুরো পাথ ব্যবহার করা হলো।
            nohup bash -c 'cd /home/${{ secrets.SERVER_USER }}/docker-with-ci-cd && /usr/local/bin/docker-compose -f docker-compose.production.yml up -d --force-recreate --remove-orphans' > docker_startup.log 2>&1 &
            
            echo "Non-blocking startup initiated. The next step will wait for containers."

      # Step 8: অপেক্ষা, মাইগ্রেশন এবং Debug Logs (5m Timeout)
      - name: Wait 90s, Run Migrations and Debug Logs
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.AWS_PORT }}
          command_timeout: 5m
          
          script: |
            cd /home/${{ secrets.SERVER_USER }}/docker-with-ci-cd
            
            # 🚨 গ্যারান্টিযুক্ত অপেক্ষা: কন্টেইনার স্টার্ট হওয়ার জন্য 90 সেকেন্ড অপেক্ষা করা হলো।
            echo "Waiting 90 seconds for all containers to fully start and stabilize..."
            sleep 90
            
            # LARAVEL কন্টেইনারের নাম বের করা
            LARAVEL_CONTAINER_NAME=$(/usr/local/bin/docker-compose -f docker-compose.production.yml ps -q backend) 
            
            if [ -n "$LARAVEL_CONTAINER_NAME" ]; then
              CONTAINER_STATUS=$(/usr/bin/docker inspect -f '{{.State.Status}}' $LARAVEL_CONTAINER_NAME)

              if [ "$CONTAINER_STATUS" = "running" ]; then
                  echo "Backend container is RUNNING. Running Migrations...";
                  echo "Waiting 10 seconds for DB connection establishment...";
                  sleep 10
                  
                  # 🚨 FIX: docker-compose এর পুরো পাথ ব্যবহার করা হলো।
                  /usr/bin/docker exec $LARAVEL_CONTAINER_NAME php artisan migrate --force;
                  /usr/bin/docker exec $LARAVEL_CONTAINER_NAME php artisan cache:clear;
                  echo "Deployment successful!";
              else
                  echo "Error: Backend container found but status is NOT RUNNING ($CONTAINER_STATUS).";
                  
                  # 🚨 ডিবাগিং: কন্টেইনারটি কেন ব্যর্থ হয়েছে তার লগ প্রিন্ট করা
                  echo "--- Printing Backend Container Logs ---"
                  /usr/local/bin/docker-compose -f docker-compose.production.yml logs backend
                  
                  # 🚨 ডিবাগিং: নন-ব্লকিং কমান্ডের আউটপুট প্রিন্ট করা
                  echo "--- Printing Non-Blocking Startup Log (docker_startup.log) ---"
                  cat docker_startup.log || echo "Startup log file not found."
                  
                  exit 1;
              fi
            else
              echo "Error: Backend container not found. Containers failed to start.";
              
              # 🚨 চূড়ান্ত ডিবাগ: নন-ব্লকিং কমান্ডের আউটপুট প্রিন্ট করা
              echo "--- Printing Non-Blocking Startup Log (docker_startup.log) ---"
              cat docker_startup.log || echo "Startup log file not found."
              
              /usr/local/bin/docker-compose -f docker-compose.production.yml logs 
              exit 1;
            fi
